import React from 'react'
import TableMutate from '../ui/TableMutate'

export default () => {
    return (
        <React.Fragment>
            <h1 className="book-title">TableMutate</h1>

            <p className="book-paragraph">Default TableMutate</p>
            <TableMutate />
        </React.Fragment>
    )
}