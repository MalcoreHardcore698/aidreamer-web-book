import React from 'react'
import Form from '../ui/Form'

export default () => {
    return (
        <React.Fragment>
            <h1 className="book-title">Form</h1>

            <p className="book-paragraph">Default Form</p>
            <Form />
        </React.Fragment>
    )
}