import React from 'react'
import GridMatch from '../ui/GridMatch'

export default () => {
    return (
        <React.Fragment>
            <h1 className="book-title">GridMatch</h1>

            <p className="book-paragraph">Default GridMatch</p>
            <GridMatch />
        </React.Fragment>
    )
}