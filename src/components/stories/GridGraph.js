import React from 'react'
import GridGraph from '../ui/GridGraph'

export default () => {
    return (
        <React.Fragment>
            <h1 className="book-title">GridGraph</h1>

            <p className="book-paragraph">Default GridGraph</p>
            <GridGraph />
        </React.Fragment>
    )
}