import React from 'react'
import TableBody from '../ui/TableBody'

export default () => {
    return (
        <React.Fragment>
            <h1 className="book-title">TableBody</h1>

            <p className="book-paragraph">Default TableBody</p>
            <TableBody />
        </React.Fragment>
    )
}