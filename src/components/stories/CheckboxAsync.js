import React from 'react'
import CheckboxAsync from '../ui/CheckboxAsync'

export default () => {
    return (
        <React.Fragment>
            <h1 className="book-title">CheckboxAsync</h1>

            <p className="book-paragraph">Default CheckboxAsync</p>
            <CheckboxAsync />
        </React.Fragment>
    )
}