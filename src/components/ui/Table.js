/*
 * COMPONENT: Table
 * 
 * MISSION: ...
 *
**/

import React from 'react'
import '../styles/Table.css'

export default ({ options }) => {
    const {

    } = options

    const classes = [
        'ui-table'
    ]

    return (
        <div className={classes.join(' ')}>
            {/* Quiet... */}
        </div>
    )
}