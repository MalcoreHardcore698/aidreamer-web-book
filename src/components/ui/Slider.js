import React from 'react'
import '../styles/Slider.css'

export default () => {
    const classes = [
        'ui-sldier'
    ]

    return (
        <div className={classes.join(' ')}>
            <p>Slider</p>
        </div>
    )
}