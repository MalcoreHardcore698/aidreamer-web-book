/*
 * COMPONENT: Modal
 * 
 * MISSION: ...
 *
**/

import React from 'react'
import '../styles/Modal.css'

export default ({ options }) => {
    const {

    } = options

    const classes = [
        'ui-modal'
    ]

    return (
        <div className={classes.join(' ')}>
            {/* Quiet... */}
        </div>
    )
}